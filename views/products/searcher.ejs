<!DOCTYPE html>
<html lang="en">
<!-- HEAD -->
<%-  include('../partials/head.ejs', { css_files: ["products"] })%>
<!-- HEAD -->

<body>
    <!-- HEADER -->
    <%-  include('../partials/header.ejs', { css_files: ["products"] })%>
    <!-- HEADER -->

    <main>
        <% if(locals.resultadoBusqueda){ %>
        <h3> Productos con la palabra: <i> <%= resultado.toUpperCase() %> </i> </h3>
        <% } %>

        <section class="product_grid">

            <% if(locals.products.length > 0){ %>

            <% products.forEach((p)=>{ %>

            <article class="product_card">
                <a href="/products/<%= product.id %>">
                    <img src="/img/products/<%= product.image %>" class="product_image">
                    <a href="#" class="product_title"><span><%= product.name %></span></a>
                </a>
            </article>
            <% if ((index + 1) % 3 === 0) { %>
            <% } %>
            <% }); %>

            <% }else{ %>

            <h4 class=""> No hay productos con la palabra: <em><%= resultado %></em></h4>

            <% } %>
        </section>
    </main>

    <!-- FOOTER -->
    <%-  include('../partials/footer.ejs', { css_files: ["products"] })%>
    <!-- FOOTER -->
</body>

</html>